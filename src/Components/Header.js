import Navigation from './Navigation';

const Header = (props) => {
  return (
    <header>
      <h1 className='h1'>Ryan Canfield</h1>
      <Navigation />
    </header>
  );
};

export default Header;
